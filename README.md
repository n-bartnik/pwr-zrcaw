
# Build application using Docker

`docker compose up`

# Install terraform

`sudo snap install terraform --classic`

# Build application using Terraform

`terraform init`
`terraform plan`
`terraform apply`


### Connect to database

`psql -h <ENDPOINT> -U chatappuser -d chatdb`